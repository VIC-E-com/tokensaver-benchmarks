Fix a panic in humantime duration parsing. Inputs whose accumulated nanoseconds carry exactly one second can panic at the maximum supported seconds value instead of returning Error::NumberOverflow. For example, parsing the largest u64 number of seconds followed by 1000ms, or by 999999999ns and 1ns, must return a normal overflow error without panicking. Preserve valid duration parsing, including carries below the limit, fractional units, and the existing public API. Add regression tests and run the existing project test suite. Work only in this repository; do not use network access or retrieve an upstream solution.
