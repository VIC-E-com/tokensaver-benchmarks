Fix glob directory traversal through symbolic links on Unix. A wildcard traversal that encounters a symlink to a directory should be able to continue matching descendants through the link, just as when the directory is named directly. Ordinary files and symlinks to files must not become traversable directories. Preserve existing error handling for broken links, ordering, matching options, and public APIs. Add regression tests for linked directories, linked files, and broken links, and run the existing tests. Work only in this repository; do not use network access or retrieve an upstream solution.
