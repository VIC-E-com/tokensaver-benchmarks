Extend humantime's RFC3339 parsing to accept the explicit UTC offset +00:00 wherever a terminal Z currently specifies UTC. Support whole seconds and fractional seconds in both the strict parser and the weak parser. Preserve the strict parser's T separator requirement and the weak parser's existing space-separated timestamps. Keep existing Z and timezone-free weak parsing behavior. Nonzero timezone offsets remain unsupported, and malformed or trailing timezone text must not be accepted. Preserve the public API, add tests, and run the project's existing tests. Work only in this repository; do not use network access or retrieve an upstream solution.
