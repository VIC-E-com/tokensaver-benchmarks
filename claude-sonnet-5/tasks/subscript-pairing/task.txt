Fix delimiter pairing for tilde runs in pulldown-cmark's inline parser when subscript is enabled but strikethrough is disabled. With only the subscript extension enabled, a single-tilde run must only pair with another single-tilde run to form a subscript, and double-tilde runs must remain literal text rather than closing or opening a subscript. For example, with subscript enabled and strikethrough disabled, the paragraph `~~This ~is~~ stricken.~` must render as literal text with the tildes kept, `a ~[x](https://x)~` must render the link inside a subscript, `b ~~[x](https://x)~~` must keep both double-tilde runs as literal text around the link, and `d ~~x ~[x](https://x)~ x~~` must render only the inner single-tilde pair as a subscript. Delimiter runs of the wrong length must not be consumed or leave dangling tree links that drop or duplicate later inline content. Preserve strikethrough, superscript and highlight behavior when those extensions are enabled, keep the public API, and keep the specs and the generated test suite consistent with the corrected behavior. Add regression coverage and run the project's existing tests. Work only in this repository; do not use network access or retrieve an upstream solution.
